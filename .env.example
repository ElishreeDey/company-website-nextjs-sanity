# blank placeholders and is committed to git. Its only job is documentation so soneone else can see what are needed `next build`, not just at runtime.

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=
NEXT_PUBLIC_SANITY_API_VERSION=
